<%= form_for(care_package) do |f| %>
  <% if care_package.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(care_package.errors.count, "error") %> prohibited this care_package from being saved:</h2>

      <ul>
      <% care_package.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :price %>
    <%= f.text_field :price %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
